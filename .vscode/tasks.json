{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "group": "build",
            "label": "matrix_experiment",
            "options": {
                "cwd": "${workspaceFolder}/06-matrix_experiment/cpp"
            },
            "type": "shell",
            "command": "make && ./plots.awk",
            "problemMatcher": "$gcc"
        }
    ]
}
